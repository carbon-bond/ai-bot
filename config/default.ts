import { Config } from './type';

const config: Config = {
    port: 4001,

    secret: 'my-secret',
    api_key: 'my-api-key',
    api_endpoint: 'http://127.0.0.1:3000/api',

    user_name: 'a',
    password: 'a'
}

module.exports = config;